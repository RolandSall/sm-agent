package io.github.scrumagent.metrics;

import io.github.springmediator.mediator.core.IQuery;

/** Mediator query: scope added/removed since the active sprint started. */
public class ScopeChangeQuery implements IQuery<ScopeChangeReport> {
}
