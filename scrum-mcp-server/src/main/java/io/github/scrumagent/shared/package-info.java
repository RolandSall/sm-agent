/**
 * Leaf module: value types and governance primitives shared across capability modules.
 * Depends on nothing; everyone may depend on it. Nothing here reaches out to Jira or any gateway.
 */
@org.springframework.modulith.ApplicationModule(allowedDependencies = {})
package io.github.scrumagent.shared;
